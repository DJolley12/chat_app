syntax = "proto3";
option go_package = "./";

service Chat {
  rpc SendMessage (ChatMessage) returns (ReceivedMessage) {}
}

message ChatMessage {
  string from_name = 1;
  string to_name = 2;
  string message_body = 3;
  bool is_encrypted = 4;
}

message ReceivedMessage {
  string name = 1;
}
